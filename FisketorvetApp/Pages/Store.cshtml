@page
@model FisketorvetApp.Pages.Shared.StoreModel
@{
}
<!--Máté-->

<h1>Stores</h1>

<div>
    <form>
        <p>
            Search: <input type="text" asp-for="Criteria" />
            <input type="submit" value="filter" />
        </p>
    </form>
</div>
<table class="table table-bordered table-hover table-striped">
    <thead>
        <tr>
            <th> Name </th>
            <th> Location </th>
            <th> Opening Hours </th>
        </tr>
    </thead>
    <tbody>
        @*@foreach (var store in Model.Stores)
        {

           <tr>
               <td><a asp-area="" asp-page="/Restaurant">@store.Name</a></td>
               <td><a asp-area="" asp-page="/Restaurant">@store.Location</a></td>
               <td><a asp-area="" asp-page="/Restaurant">@store.OpeningHours</a></td>
           </tr>
        }*@

        @foreach (var store in Model.Stores)
        {

            <tr>
                <td><a asp-page="/Products" asp-route-name="@store.Name">@store.Name</a></td>
                <td>@store.Location</td>
                <td>@store.OpeningHours</td>

            </tr>

        }



        <div class="container">
            <div class="row">
                @foreach (var store in Model.Stores)
                {
                <div class="card mb-3 mx-auto col-12 col-md-6 col-lg-3">
                    <div class="card-body">
                        <a asp-page="/Products" asp-route-name="@store.Name">@store.Name</a>
                        <h5> @store.Location</h5>
                        <h5>@store.OpeningHours</h5> 
                    </div>
                </div>
                }

            </div>
        </div>


    </tbody>
</table>